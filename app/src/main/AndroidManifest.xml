<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javaandroidsummary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SintClassMeth"></activity>
        <activity android:name=".git_config" />
        <activity android:name=".git" />
        <activity android:name=".Activity_activity" />
        <activity android:name=".adapters_and_lists" />
        <activity android:name=".OsnEleleInterface" />
        <activity android:name=".Android" />
        <activity android:name=".TextForm" />
        <activity android:name=".OperatorsLayer" />
        <activity android:name=".Operators" />
        <activity android:name=".practica" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>